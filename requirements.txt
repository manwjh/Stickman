# ============================================
# AI Stick Figure Story Animator - Dependencies
# ============================================

# Core Web Framework
Flask==3.0.0
Flask-CORS==4.0.0

# Configuration & Environment
python-dotenv==1.0.0
PyYAML==6.0.1

# AI/LLM Integration
litellm>=1.57.0
pydantic>=2.10.0

# HTTP & Networking
requests==2.31.0

# GIF Export (requires system cairo library)
# macOS: brew install cairo
# Ubuntu: sudo apt-get install libcairo2-dev
Pillow>=10.0.0
cairosvg>=2.7.0

# Alternative GIF Export (pure Python, no system dependencies)
# Uncomment if you prefer not to install cairo
# svglib>=1.5.1
# reportlab>=4.0.0

# Production Server (optional, for deployment)
gunicorn==21.2.0
gevent>=23.9.1

# ============================================
# Installation Instructions
# ============================================
# 
# 1. Basic installation:
#    pip install -r requirements.txt
#
# 2. For GIF export, install cairo first:
#    macOS:   brew install cairo
#    Ubuntu:  sudo apt-get install libcairo2-dev
#    Windows: Download from https://cairographics.org/
#
# 3. Production deployment:
#    pip install -r requirements.txt
#    gunicorn and gevent are included for production
# ============================================
